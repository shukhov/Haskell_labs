--задача 4.6
-- (1) каждая запятая, разделяющая элементы списка верхнего уровня,
-- заменятеся двоеточием (":"), после которой ставится открывающаяся 
-- скобка; соответствующая закрывающаяся скобка ставится перед скобкой,
-- которая закрывает верхний уровень списка;
-- (2) последний элемент (допустим, а) списка заменяется: (а: []);  
-- (3) правила, написанные выше, применяются и к подспискам (списки 
-- второго, третьего и т.д. уровня).   